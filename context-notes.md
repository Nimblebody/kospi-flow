# 컨텍스트 노트

## 2026-08-27 · 테마 순위가 삼성전자 하나에 지배되는 문제

### 증상
첫 리포트에서 상위 5개 테마가 전부 삼성전자·삼성전기를 최상위 종목으로 물고 있었다.
아이폰 / 태블릿PC / 온디바이스AI / 폴더블폰 / 갤럭시부품 — 사실상 같은 수급을 다섯 번 센 셈.

### 원인 (실측)
87개 테마를 전수 조사한 결과 원인이 셋으로 갈렸다.

1. 집중도. 상위 10개 테마 전부 1등 종목이 순매수의 38~73% 를 차지.
   `CXL` 은 삼성전자가 72.7%. 테마가 강한 게 아니라 종목이 강한 것.
2. 표본. `반도체/반도체장비` 는 구성종목 110개 중 8개만 데이터가 잡힘 (7.3%).
   KIS 수급 API(FHPTJ04400000)가 상위 30종목만 돌려주기 때문. 이건 API 한계라 못 늘린다.
3. 중복 계산. 삼성전자 하나가 16개 테마에 소속. +2,987억이 16번 계산된다.

분포. top1_share 가 50% 미만인 테마는 87개 중 22개, 40% 미만은 11개.
데이터가 2종목뿐인 테마가 35개로 최다.

### 선택한 방향
금액은 그대로 두고 대표성 지표를 추가해 상위 노출에서만 거른다.

검토했으나 안 택한 것.
- 겹치는 테마 묶기. 화면은 깔끔해지지만 묶는 임계값이 자의적이고 원래 구분되던 테마가 사라진다.
- 지분 분할 배분 (종목 순매수 / 소속 테마 수). 중복 계산은 근본적으로 없어지지만
  금액이 '실제 순매수' 가 아니게 되어 "아이폰 테마 +484억" 같은 해석 불가능한 숫자가 나온다.

### 설계 결정
- `top1_share` 의 분모는 net 합이 아니라 **|net| 합**.
  net 합은 0 근처에서 비중이 폭발하고 부호가 섞이면 의미가 없어진다.
- `themes` 전체 리스트는 net 내림차순 그대로 유지한다. 원본을 버리지 않는다.
  거르는 건 `themes_top` / `themes_bottom` / 헤드라인 등 '보여주는 자리' 뿐이다.
- featured 가 하나도 없는 날은 전체 리스트로 폴백한다. 화면이 비는 것보다 낫다.
- 임계값은 감이 아니라 위 분포에서 잡았다. 0.5 는 22개가 남는 지점.

## 2026-08-27 · 윈도우 tzdata 누락
`config.py` 의 `ZoneInfo("Asia/Seoul")` 이 로컬 윈도우에서 터졌다.
윈도우에는 시스템 시간대 DB 가 없다. GitHub Actions 는 우분투라 드러나지 않던 버그.
`requirements.txt` 에 `tzdata; sys_platform == "win32"` 로 조건부 추가.

## 2026-08-27 · 키 보관
`.env` (로컬, gitignore) 와 GitHub Secrets 두 군데. 저장소는 public 이지만 키는 코드가 아니라 안전.
`.env.example` 은 추적되는 견본 파일이므로 실제 값을 넣지 않는다. 한 번 실수해서 되돌렸다.

### 적용 후 결과 (2026-08-27 데이터)
87개 중 65개가 상단 노출에서 빠졌다. 상위권이 이렇게 바뀌었다.

    before) 아이폰 / 태블릿PC / 온디바이스AI / 폴더블폰 / 갤럭시부품  ← 전부 삼성전자
    after ) 아이폰 / 태블릿PC / 온디바이스AI / 한미FTA수혜 / 한국판뉴딜 /
            북한전력설비 / 전력기기 / 전기차                        ← 주도주가 갈림

삼성 계열이 5개 연속에서 3개로 줄고, LS ELECTRIC·삼성SDI 가 주도하는 테마가 올라왔다.
남은 한계. 아이폰과 태블릿PC 는 구성종목이 60% 겹쳐 여전히 나란히 뜬다.
이건 '겹치는 테마 묶기' 를 해야 풀리는데 이번엔 택하지 않았다. 화면에 1등 종목 비중을
적어 두었으니 읽는 사람이 판단할 수 있다.

### 작업 중 발견한 버그
`themes_bottom` 을 `featured[-8:]` 로 잘랐더니 순매수 +35억 인 테마가
'자금이 빠져나간 테마' 칸에 떴다. 대상이 87개에서 22개로 줄면서 드러난 문제다.
개수가 아니라 부호(net_eok < 0)로 자르도록 고쳤다.

## 2026-08-27 · 겹치는 테마 묶기 (2차)

1차에서 남긴 한계(아이폰↔태블릿PC 가 나란히 뜸)를 이어서 처리했다.

### 임계값을 100%(완전 포함)로 잡은 이유
겹침 비율을 100 / 80 / 60% 로 두고 실측한 결과.

    100% -> 대표 56개 (흡수 31개)
     80% -> 대표 56개 (흡수 31개)   ← 100% 와 완전히 동일
     60% -> 대표 42개 (흡수 45개)

80% 가 100% 와 같은 이유. 테마당 수급이 잡힌 종목이 2~5개뿐이라 겹치면 대개
완전히 겹치고, 아니면 절반 이하로 떨어진다. 중간 지대가 없다.

60% 는 온디바이스AI(커버리지 8/13 로 가장 좋음)까지 아이폰에 흡수했고,
북한전력설비가 전력기기의 대표가 되는 등 이름이 앞뒤가 안 맞았다.
100% 는 '전부 포함된다' 는 사실 관계라 임계값을 고를 여지 자체가 없다.

### 대표(호스트) 선정을 featured 우선으로 한 이유
금액순으로만 호스트를 고르면, 대표성 없는 큰 테마가 호스트가 되고
그 아래 깔린 멀쩡한 테마까지 featured 필터에서 통째로 사라진다.
실제로 재현했다.

    금액순만      -> ['쏠린큰테마']              ← 고른작은테마가 증발
    featured 우선 -> ['고른작은테마', '쏠린큰테마']

featured 를 먼저 훑으면 featured 테마는 다른 featured 테마에만 흡수된다.
test_merge_prefers_featured_theme_as_host 가 이걸 지킨다.

### 체이닝을 피한 방법
A~B, B~C 가 겹친다고 A와 C를 한 덩어리로 묶는 연결요소 방식은 쓰지 않았다.
이미 뽑힌 대표와 비교해 흡수하는 방식이라 A와 C가 안 겹치면 따로 남는다.

### 비용 (실측)
묶기 계산 0.3~0.5ms. analyze() 전체가 5ms.
JSON 은 66.7KB -> 75.4KB (지표 필드 + merged 배열, 두 작업 합산).
파이프라인 전체는 KIS 호출로 수십 초가 걸리므로 무시 가능하다.

### 결과
87개 중 30개가 묶였다. 태블릿PC·CXL·SSD·AMOLED 가 아이폰 아래로 들어갔고,
빅데이터·웹툰·인터넷서비스·핀테크·그린홈 이 한국판뉴딜 아래로 들어갔다.
온디바이스AI 는 성격이 다르므로 따로 남았다 (의도한 대로).

## 2026-08-27 · 확정치 전환 + 과거 백필 (3차)

자금 이동을 보려면 전 거래일 리포트가 있어야 하는데 오늘치뿐이었다.

### 왜 그냥 백필만 하면 안 됐나
지금 수급을 모으던 FHPTJ04400000(가집계)은 날짜 파라미터가 없다. 과거를 못 받는다.
날짜를 받는 건 FHPTJ04160001(일별 확정) 뿐인데, 두 API 의 값이 실제로 다르다.

    삼성전자 2026-08-27    외국인      기관
      가집계              +1,506억   +1,482억
      일별확정            +3,700억     -252억   ← 기관은 부호까지 반대

단위 문제가 아니다. 수량×종가로 교차검증했고 둘 다 백만원 단위가 맞다.
가집계는 장중 어림치, 확정치는 정산 후 값이라 원래 다르다.
자금 이동은 어제 대비 증감이라 두 날의 출처가 다르면 출처 차이가
자금 이동으로 둔갑한다. 그래서 final 단계를 확정치로 통일했다.
flash(16:10)는 확정치가 아직 안 나오므로 가집계 속보로 남겼다.

### 뜻밖의 이득
확정 API 는 조회한 종목 전부를 준다. 상위 30종목 제약이 사라지면서
1차에서 문제였던 커버리지가 통째로 해결됐다.

    테마 수     87개 -> 243개
    한미FTA수혜  8/26 -> 21/21 (100%)
    전력기기     4/6  -> 5/5   (100%)

### 성능 — 예측이 틀렸던 지점
'초당 8콜이니 752종목에 1.6분' 이라고 계산했는데 실제로는 11분이 걸렸다.
레이트 리미터는 상한만 걸 뿐이고 순차 호출이라 왕복 지연(콜당 0.9초)이
그대로 쌓인다. ThreadPoolExecutor 로 8개를 동시에 띄워 3분 15초로 줄였다.
그래도 1.6분에는 못 미친다. 지연이 완전히 겹치지는 않기 때문이다.
requests.Session 의 기본 커넥션 풀이 10이라 pool_maxsize 도 함께 올렸다.

### 작업 중 발견한 버그 셋
1. 백필이 오늘 날짜까지 덮으면서 헤드라인의 코스피 지수 줄이 사라졌다.
   과거 지수를 주는 API 가 없어 market 을 비워두기 때문. 마지막 날만
   collect_market 으로 실시간 조회하게 고쳤다.
2. 휴장일 다음날 자금 이동이 빠졌다. _prev_business_day 가 주말만 건너뛰고
   대체공휴일(8/17)은 모른다. 달력 계산을 버리고 store.load_history 로
   '실제로 있는 가장 최근 리포트' 를 쓰게 했다. run() 에도 같은 버그가 있었다.
3. 커버리지 분모가 거짓말을 했다. 태블릿PC 가 화면에 '4/23종목' 으로 떴는데
   23개 중 18개는 코스닥이라 애초에 조회 대상이 아니다. 실제로는 4/5.
   전체로도 members_total 합계 4,978 중 조회 대상은 1,831(36.8%)뿐이었다.
   스냅샷에 universe 를 실어 분모로 쓰게 고쳤다. 가집계에는 유니버스 개념이
   없으므로 없으면 예전대로 전체를 센다.

### 정리한 것
add_retail_flows 는 확정치가 개인 수급을 함께 주면서 쓰이지 않게 되어 제거했다.
_prev_business_day 도 마찬가지.

### 남은 문제
리포트가 77KB -> 190KB 로 커졌다. 매일 커밋하므로 연 250영업일에 약 46MB.
대시보드는 latest.json 만 읽고, 과거 파일은 자금이동·연속일수 계산에
테마 이름과 금액만 쓴다. 과거분을 얇게 저장하면 대부분 줄일 수 있다.

## 2026-08-27 · 과거 리포트 얇게 저장 (4차)

### 무엇이 컸나
190KB 중 themes 가 90%(171KB), 그 안에서도 stocks(테마별 상위 5종목 표)가 107KB.
243개 테마마다 같은 대형주를 중복 저장하고 있었다.

### 구조
    web/data/<날짜>.json   얇게 (24KB)  - 자금 흐름 시계열
    web/data/latest.json   전체 (195KB) - 대시보드가 읽는 파일

대시보드는 latest.json 하나만 fetch 하고, 날짜별 파일은 load_history 를 통해
자금 이동(compute_rotation)과 연속일수(compute_streaks)에만 쓰인다.
그 둘이 실제로 읽는 건 테마 이름과 net_eok 뿐이다.

남긴 것. date / stage / collected_at / theme_source / headline / investors /
indices / rotation / themes(name, net_eok, foreign_eok, institution_eok, streak).
외국인·기관 분해와 시장 전체 투자자 합계를 남긴 건 나중에 시계열 차트를
그릴 때 필요해서다. rotation 도 남겼다. 이 앱이 보여주려는 것 자체라서.

버린 것. 테마별 stocks, themes_top/bottom, top_buy/sell, spikes, sectors.

### 되돌릴 수 없다
KIS 는 30일치만 돌려준다. 30일이 지나면 종목별 상세를 다시 만들 방법이 없다.
그래서 '자금 흐름' 에 해당하는 시계열은 최대한 남기고 화면 표시용만 버렸다.

### 검증
얇게 만든 보관본으로 오늘 분석을 다시 돌려 결과가 같은지 확인했다.
자금이동 짝 8개 전부 동일, 연속일수 불일치 0개.

    13일치  2.43 MB -> 0.31 MB (12.8%)
    파일당   192 KB -> 24 KB
    연 250영업일 누적 예상  46 MB -> 6 MB

## 2026-08-27 · 과거 날짜 보기 (5차)

### 6개월 단위 축약이 용량을 줄이지 못하는 이유
git 은 파일을 고쳐도 예전 버전을 지우지 않는다. 나중에 얇게 다시 쓰면
저장소가 줄어드는 게 아니라 새 버전이 하나 더 쌓인다. 60영업일을 실제로
커밋해 재봤다.

    방식                        작업트리    .git    연 250일 환산
    얇게만                       1.3 MB   0.3 MB      ~1 MB
    전체를 계속                   10.7 MB   2.1 MB      ~9 MB
    전체 유지 후 얇게 (제안 방식)     4.5 MB   2.1 MB      ~9 MB

작업트리는 절반 이하로 줄지만 .git 은 똑같다. 클론 용량은 .git 이 정한다.

### 앞서 말한 '연 46MB' 는 과한 추정이었다
그건 파일 크기 단순 합계였다. git 델타 압축을 거치면 전체를 계속 저장해도
연 9MB 수준이다. 즉 용량은 애초에 문제가 아니었다.

### 그래서 저장 전략이 아니라 기능으로 바꿨다
FULL_REPORT_DAYS(125영업일 ≈ 6개월) 안의 리포트는 전체본으로 두고,
대시보드에 날짜 선택을 달아 그날 화면으로 되돌아가 볼 수 있게 했다.
기간이 지나면 축약하되, 그때도 테마 순매수와 자금 이동은 계속 보인다.

외부 스토리지(구글 드라이브/네이버 MYBOX)는 검토했으나 접었다.
정적 페이지가 fetch 로 읽어야 하는데 드라이브는 직접 링크에 CORS 를 주지 않아
프록시가 필요하고, MYBOX 는 공개 API 자체가 없다. 연 1~9MB 를 위해 할 일이 아니다.

### 축약본에 featured 를 남긴 이유
축약본만 있는 날짜에서도 화면이 테마 순위를 다시 뽑아야 한다. featured 가 없으면
대형주 하나가 끌고 가는 테마(갤럭시부품·OLED·LED·폴더블폰 등)가 상위로 올라와
최신 화면과 순위가 어긋난다. 실제로 그렇게 나오는 걸 확인하고 추가했다.
필드 하나에 2.4KB 지만 일관성이 그만한 값어치를 한다.

### 검증에서 잡은 버그 둘
1. themeTable 이 축약본에서 undefined.toFixed() 로 터졌다. 평균등락·종목수·
   1등비중은 축약본에 없다. 전부 '—' 로 표시하게 고쳤다.
2. deriveBuckets 가 'featured 가 false' 와 '필드 자체가 없음' 을 구분하지 못했다.
   옛 축약본에서 필터가 통째로 무력화된다. 필드 유무를 먼저 본다.

축약본으로 렌더한 테마 순위가 전체본 themes_top 과 이름까지 정확히 일치하는 것을
node 로 실행해 확인했다.

## 2026-08-27 · 자금 이동 개수 확대 + 업종 구성종목 (6차)

### 자금 이동
경로 8 -> 13개, 유입/유출 각 5 -> 10개. config.ROTATION_TOP_THEMES /
ROTATION_MAX_PAIRS 로 뺐다.

경로 개수는 상한만 올린다고 늘지 않는다. 유입·유출을 크기순으로 그리디하게
짝짓는 방식이라, 양쪽 금액이 딱 맞아떨어지면 1:1 로 소진돼 top_n 개만 나온다.
어긋나야 쪼개지며 늘어난다. 테스트 픽스처를 처음에 금액이 같게 만들었다가
이걸로 한 번 헛발질했다. test_rotation_pair_count_is_capped 가 두 경우를 다 잡는다.

저장된 13일치는 KIS 호출 없이 재계산했다. 리포트마다 themes 가 들어 있어
전날 것과 비교하면 compute_rotation 을 그대로 돌릴 수 있다.

### 업종 구성종목
업종시세 API(inquire-index-category-price)는 지수만 주고 구성종목을 안 준다.
종목-업종 매핑은 코스피 종목 마스터 뒤쪽 228자에 들어 있는데, 오프셋을
추측하지 않고 전 구간을 4자리 창으로 훑어 API 의 bstp_cls_code 와 가장 많이
일치하는 자리를 찾아 확정했다.

    [4:8]  지수업종 대분류  939종목 / 11종류  — 제조·금융처럼 거칠다
    [8:12] 지수업종 중분류  592종목 / 13종류  — 화학·전기·전자처럼 화면과 맞물린다

중분류 우선, 없으면 대분류로 폴백. 이 조합으로 수급 유니버스 752종목 중
748개(99.5%)에 업종이 붙고 24종류로 갈린다.

'종합' '대형주' '고배당50' 'VKOSPI' 등은 업종이 아니라 지수·규모구분이라
구성종목이라는 게 없다. 이런 칸은 아예 버튼으로 만들지 않는다.
화면 38개 중 24개가 눌린다.

주의. 마스터 파싱은 줄 끝 처리에 민감하다. row[len(row)-228:] 로 뒤에서 세므로
rstrip 방식이 바뀌면 오프셋이 통째로 밀린다. load_stock_names 는 아예 strip 을
안 하고, load_stock_sectors 는 rstrip("
") 을 쓴다. 둘의 기준이 다르니
건드릴 때 주의할 것.

## 2026-08-28 · 예약 실행이 새벽에 깨어나 실패한 건

### 사실 확인
Actions API 로 실행 기록을 봤다. 예약 실행은 지금까지 딱 한 번 돌았고 실패했다.

    생성 2026-08-27 19:53 UTC (KST 08-28 04:53)  event=schedule  → failure
    실패 단계: '리포트 생성' ('키 확인' 은 통과)

크론은 07:10 / 09:30 UTC (KST 16:10 / 18:30) 둘뿐이다. 19:53 UTC 와 어느 쪽도
맞지 않는다. 새로 추가된 pages / probe-us / telegram-test 워크플로에는
예약이 아예 없다. 미장 마감에 맞춘 스케줄 같은 건 없다.

GitHub 스케줄러가 크게 밀려서 엉뚱한 시각에 발사한 것으로 본다. 시각을
조정해서 막을 수 있는 문제가 아니라, 언제 깨어나도 죽지 않게 만들어야 한다.

### 고친 것
빈 수급을 무조건 sys.exit(1) 로 처리하던 걸 세 갈래로 나눴다.

    수급이 있을 수 없는 시점 (미래 날짜 / 코스피 마감 15:30 전)
        -> 수집도 하지 않고 exit 0. 752콜 3분을 낭비할 이유가 없다.
    마감은 지났지만 확정 수급이 아직 안 올라온 구간 (~16:10)
        -> 수집은 하되 비어 있으면 exit 0. 정상이다.
    16:10 이후인데도 비어 있음 / 지난 거래일인데 비어 있음
        -> exit 1. 이건 진짜 알려야 한다.

config.MARKET_CLOSE_KST(15,30) 와 DATA_READY_KST(16,10) 두 값으로 가른다.

### 놓칠 뻔한 것
처음엔 '오늘인데 장중인가' 로만 판단해서, 미래 날짜를 주면 오늘 장이 끝난 뒤에는
그대로 752콜을 긁으러 갔다. 실제로 --date 20260901 로 돌려보고 발견했다.
두 판정 함수의 날짜 분기를 같은 모양(미래/과거/오늘)으로 맞췄고,
test_two_gates_are_consistent 가 둘이 서로 모순되지 않는지 하루 전체를 훑어 본다.

## 2026-08-28 · 미국 시세가 현지 날짜와 하루 어긋난 건

### 증상
KST 8/28 에 돌린 리포트의 us.as_of 가 8/28 로 찍혔다. 그 시각 현지는 8/28 새벽
04:24 로 장이 열리지도 않았다. 엔비디아가 화면에 -1.06% 로 나왔는데
실제로는 8/27 에 +8.74% 로 끝났다.

### 원인
collect_us 는 날짜를 지정해 요청하지 않는다. 일봉 시계열의 첫 행을 쓴다.
그런데 KIS 해외주식 일봉(HHDFS76240000)이 아직 안 끝난 세션도 한 행으로 준다.

    KIS  20260828  clos 225.56  high 227.98   <- 프리마켓 진행 중
         20260827  clos 227.98               <- 실제 마감
    야후 20260827  227.98                     <- 8/28 행이 아예 없다

8/28 행의 high 가 8/27 종가와 같은 것이 시간외 값이라는 표시다.
해외지수(FHKST03030100)는 이런 행을 안 줘서 SPX 는 멀쩡했다. 그래서 섹터 12개와
종목 20개만 어긋났고, as_of 는 그 32개가 다수결을 이겨 8/28 이 됐다.

### 고친 것
last_closed_us_date() 로 '마감이 끝난 가장 최근 현지 날짜' 를 구하고,
그보다 뒤인 행을 버린다(_closed_only). _series 와 _fetch_yahoo 두 관문에
걸어 두어 지수·주식·야후 경로가 전부 지나간다.

시간대는 ZoneInfo("America/New_York") 를 쓴다. 고정 오프셋(-4)으로 두면
서머타임이 끝나는 11월에 한 시간 틀어진다. 테스트가 1월 날짜로 그걸 잡는다.

    KST 8/28 17:25 -> ET 8/28 04:25 (장 전)   -> 20260827
    KST 8/28 06:30 -> ET 8/27 17:30 (마감 후) -> 20260827
    KST 8/29 00:30 -> ET 8/28 11:30 (장중)    -> 20260827

### 안 건드린 것
비트코인은 업비트 KST 일봉이라 이 필터를 안 탄다. 24시간 거래라 '미완료 세션'
이라는 개념이 없고, 현재가를 보는 게 자연스럽다. 화면에 '업비트 · 전일 대비'
라고 적혀 있다. 그래서 미국 항목이 8/27 일 때 비트코인만 8/28 이다.

## 2026-08-28 · 업종별 뉴스 검색을 안 한 이유

### 물음
업종 요약(상위 10 / 하위 10)에 기사를 붙이려면 업종마다 따로 검색해야 하나.
'업종명이 거래소 분류라 기자들이 쓰는 말이 아니다' 까지는 짐작이 갔는데,
그러면 사람 말로 된 사전을 만들면 되는지가 남았다. 세 방식을 다 재 봤다.

관련성 판정은 눈대중 대신 **제목에 그 업종 구성 종목 이름이 있는지**로 했다.
우리 리포트가 업종별로 수급이 잡힌 종목 10개를 들고 있어서 가능했다.

### 실측 (2026-08-28, 구성 종목이 붙은 24개 업종)

1. **거래소 업종명 그대로** `<업종명> 주가` × 24회
   업종당 그날 기사 2.7건. 98건 중 관련 38건 (39%).
   그런데 관련 기사가 몇 업종에 몰렸다 — 건설 14건 중 13건, 제약 15건 중 9건,
   증권 29건 중 11건. 이유는 **회사 이름에 업종명이 들어가기 때문**이다
   (대우건설, 신풍제약, 한국투자증권). 업종 기사가 아니라 종목 기사였다.

2. **기사에 나오는 말로 만든 사전** (반도체주·식품주·시멘트주·엔터주 …) × 24회
   60건 중 관련 6건 (**10%**). 기존보다 나빴다.
   '반도체주' 는 16건이 왔는데 삼성전자·SK하이닉스가 제목에 없는 미국·ETF
   기사였다. 업종어로 검색하면 국내 그 업종 기사가 아니라 시장 해설이 온다.

3. **공용 풀 + 종목 이름으로 배정** (검색 추가 없음)
   지수 기사 위주 40건 → 1개 업종만 배정.
   '특징주' 검색어 하나를 더해 70건으로 넓히니 **4~5개 업종**.
   '급등주'·'급락주' 를 더 넣어도 5개에서 안 늘었다.

### 결론
검색어 문제가 아니었다. **하루에 업종 단위로 기사가 나오는 업종이 3~5개뿐**이다.
음식료·담배, 종이·목재, 비금속은 어떤 검색어로도 0건이었다. 원래 뉴스가 안 나오는
업종에 검색을 24번 돌려도 빈손이거나 남의 기사가 온다.

### 선택한 방향
- 업종별 검색을 하지 않는다. 공용 풀에 `특징주` 하나만 더한다 (RSS 1회 추가)
- 기사가 붙은 업종만 이유를 쓰고, **나머지는 어느 종목이 끌었는지까지만** 쓴다.
  없는 이유를 만들지 않는 원칙은 지수 해설과 같다
- 20개 업종을 **한 번의 호출**로 묶는다. 업종마다 따로 부르면 3.5배 비싸고,
  서로를 모르는 채 쓰여 '반도체에서 빠져 화학으로' 같은 문장이 안 나온다

### 비용 (실측 토큰 기준)
한국어는 글자당 0.979토큰이었다 (실측 4,342자 = 4,251토큰). 이 비율로 계산.

    지수 해설 2콜            월 $0.95   1,313원
    + 업종 요약 1콜          월 $0.63     872원
                             ─────────────────
                             월 $1.58   2,185원

    (업종별 20콜 · 기사 25건씩이면 월 $3.14 · 4,333원. 2.6배를 더 내고
     사는 것이 대부분 노이즈였다)

### 작업 중 발견한 버그
종목 이름 `SK` 가 `SK하이닉스` 가 든 제목에 걸려 금융 업종으로 배정됐다.
긴 이름부터 맞추고 첫 매칭에서 멈추는 것으로 고쳤다.

### 미국 섹터에도 같은 것을 (같은 날 추가)
미국 섹터는 ETF 등락률만 오고 구성 종목이 없다. 관심 종목 20개를 섹터에 붙여
국내와 같은 모양으로 맞췄다. 엔비디아·브로드컴은 실제로 기술(XLK)에도 들어 있지만
반도체로 보낸다 — 국내 수급이 따라 움직이는 자리가 그쪽이고, 한 종목을 두 섹터에
세면 같은 움직임을 두 번 센다.

12개 섹터 중 6개(에너지·유틸리티·소재·산업재·부동산·필수소비재)는 붙는 종목이
없어 ETF 등락률만 남는다. 그 칸은 이유를 비운다.

    미국 섹터 1콜   월 $0.37  506원
    합계            월 $1.95  2,689원

화면 라벨은 '오른/내린' 이 아니라 '등락률 상위/하위' 로 적는다. 전부 상승한 날에
하위 칸에 +1.2% 가 들어가는 것을 미리보기에서 봤다.

## 2026-09-02 · 뉴스 탭

전날 증시 기사를 모아 한 편으로 요약하고 중요 기사 20건에 링크를 건다.
01:30 KST 에 따로 돈다(수급 리포트와 별개 워크플로).

### 기사 출처를 두 갈래로 둔 이유
구글 뉴스 RSS 는 출처가 넓지만 링크가 news.google.com 중간 페이지다.
진짜 주소를 서버에서 뽑으려고 두 가지를 시도했고 둘 다 실패했다.

  - CBMi... 토큰 base64 디코딩 -> 주소가 안 들어 있다 (199바이트 중 http 없음)
  - 중간 페이지 HTML 파싱 -> 자바스크립트가 따로 받아온다. 본문에 없다

그래서 국내 언론사 RSS 를 먼저 쓴다. 실측으로 8곳이 열렸다(연합·한경 finance/
economy·머니투데이·아시아경제·뉴시스·조선비즈·연합인포맥스). 매일경제는 403,
이데일리는 연결 끊김, 서울경제·파이낸셜뉴스는 404 였다.

수집 결과 247건 중 직접링크가 69% 였다. 나머지는 구글 경유로 두되 화면에
'구글 뉴스 경유' 라고 밝힌다.

머니투데이·아시아경제는 종합 피드라 스포츠·연예가 섞인다. 제목 키워드로 거른다.

### 모델에게 제목을 다시 쓰게 하지 않는다
번호(index)만 고르게 하고 실제 기사와는 파이썬이 맞춘다. 제목을 지어내는 일이
원천적으로 불가능해진다. 범위 밖·중복·정수가 아닌 번호는 버린다.
tests/test_news.py 가 그 네 경우를 다 잡는다.

### 시각
01:30 KST 에 도는데, 그 시각의 '어제'가 직전 거래일이라 마감 정리 기사가
다 들어와 있다. 수집 창은 어제 00:00 부터 지금까지다.

### Worker
크론마다 다른 워크플로를 부르도록 JOBS / CRON_JOB 을 뒀다. 뉴스는 전날치를
만들어서 '이미 끝났나' 판정의 기준 날짜가 리포트와 하루 다르다.
news.yml 은 stage 입력을 받지 않으므로 dispatch 본문에서 뺀다(422 방지).
